//given an integer array having n elements, find the number of occurrences of 1 and remove the  elememnt at the index which is equal 
//to the no of occurrences of 1 print the modified array; given that count of 1<n
#include <bits/stdc++.h>
using namespace std;
int main(){

    int n;
    cin>>n;
    vector <int> arr(n);
    for (int i=0;i<n;i++){
        cin>>arr[n];
        


    }
    int freq= count(arr,arr+n,1);
    

    
    
    
}