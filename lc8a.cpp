#include <bits/stdc++.h>
using namespace std;
int main(){

    string text= "programming";
    string s= "programming";
    string part1=text.substr(0,4);
    string part3=text.substr(6);
    cout<<text;
    cout<<endl;
    cout<<part1<<" "<<part3;

    //string rotation
    // n ---> length of string k----> rotation value
    // string s1=s.substr(n-k,k)+s.substr(0,n-k);    ---->right rotation
    // string s1=s.substr(k,n-k)+s.substr(0,k);    ---->left rotation

    // k=k%n;
    // reverse(old_str.begin()old_str.end()); ---> string reversal
    
,
} 